import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  button: {
    // width: '100%',
    justifyContent: 'center',
    borderRadius: 50,
  },
  buttonBox: {
    flexDirection: 'row',
    alignItems: 'center',
  },
  toggleButtonText: {
    fontWeight: '500',
  },
});
